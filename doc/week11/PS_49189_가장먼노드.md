# 프로그래머스 가장 먼 노드

[문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/49189)

## 접근
- `ArrayList` 자료구조를 이용한 인접 리스트로 그래프 입력 받음
- BFS 알고리즘을 사용하여 1번 노드 기준으로 각 노드들까지의 최단 거리를 구함
- 멀리 떨어진 노드의 수를 구함

### 정답 코드
```
import java.util.ArrayDeque;
import java.util.ArrayList;
import java.util.Queue;

public class Solution {
    public int solution(int n, int[][] edge) {
        int answer = 0;

        ArrayList<ArrayList<Integer>> graph = new ArrayList<>();
        for (int i = 0; i <= n; i++) graph.add(new ArrayList<>()); //0번지 버림

        for (int[] e : edge) {
            graph.get(e[0]).add(e[1]);
            graph.get(e[1]).add(e[0]);
        } //for - insert graph

        Queue<Integer> que = new ArrayDeque<>();
        boolean[] visited = new boolean[n+1];
        int[] dist = new int[n+1];

        que.add(1);
        visited[1] = true;

        while (!que.isEmpty()) {
            int cur = que.poll();

            for (int next : graph.get(cur)) {
                if (!visited[next]) {
                    visited[next] = true;
                    dist[next] = dist[cur] + 1;
                    que.add(next);
                }
            } //for - next
        } //while - bfs

        int max = 0;
        for (int d : dist) {
            if (d > max) {
                max = d;
                answer = 1;
            } else if (d == max) answer++;
        } //for - max

        return answer;
    }
}
```

### 회고
- 어떻게 접근해야 할 지는 알고 있었으나 오랜만에 해서 그런지 코드를 구현하는 것은 쉽지 않았다.
- 코드가 술술 나오도록 꾸준히 연습해야겠다.
